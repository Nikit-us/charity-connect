plugins {
    id("java-conventions")
}

group = "com.mdl"
version = "1.0-SNAPSHOT"

dependencies {
    // Только pure Java зависимости
    // Примеры того что сюда пойдёт: mapstruct, apache commons и т.д.
}